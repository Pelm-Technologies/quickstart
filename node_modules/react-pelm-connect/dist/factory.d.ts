import { PlaidLinkOptions } from './types';
export interface PlaidFactory {
    open: Function;
    exit: Function;
    destroy: Function;
}
/**
 * Wrap link handler creation and instance to clean up iframe via destroy() method
 */
export declare const createPlaid: (options: PlaidLinkOptions) => {
    open: () => void;
    exit: (exitOptions: any, callback: Function) => void;
    destroy: () => void;
};
