import * as React from "react";
export interface Props {
    shouldShowBackButton?: boolean;
    onBack: () => void;
    onContinue: () => void;
    onDismiss: () => void;
}
export declare class FlowStepScreenStructure extends React.Component<Props, {}> {
    private hasBackButton;
    private backButton;
    private closeButton;
    render(): React.ReactNode;
}
