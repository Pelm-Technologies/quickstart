import * as React from 'react';
import { Config } from './types/index';
declare type Props = {
    config: Config;
    className?: string;
    children?: React.ReactNode;
};
export declare const ConnectButton: (props: Props) => JSX.Element;
export {};
